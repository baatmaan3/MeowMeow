<%- include('../partials/metadata') %>

<%- include('../partials/navbar', { profile }) %>

<main class="pt-16">
  <div class="flex flex-col justify-center mx-5">
    <section class='max-w-72 w-full flex  justify-start mx-24'>
      <article class='w-full'>
        <h5 class='mx-5 mt-5 mb-2 text-xs font-bold tracking-tight text-gray-400 dark:text-white'>ITEMS YOU HAVE PURCHASED
        </h5>
        <div class="flex gap-2">
          <%- include('../partials/product', { product, type: 'own' }) %>
        </div>
      </article>
    </section>
  </div>
  </div>
</main>
<!-- <script>
      const copy = content => {
        navigator.clipboard.writeText(content);

        alert("Copied Link: " + content);
      }
    </script> -->
<%- include('../partials/close') %>
